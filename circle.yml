machine:
  python:
    version: 3.5.1
  node:
    version: 5.11.1
  environment:
    NODE_ENV: production

dependencies:
  pre:
    - make
  cache_directories:
    - env
    - node_modules
    - pelican_plugins

test:
  override:
    - make test
