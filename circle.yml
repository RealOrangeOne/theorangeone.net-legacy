machine:
  python:
    version: 3.4.2
  node:
    version: 5.10.1
  environment:
    NODE_ENV: production

dependencies:
  pre:
    - ./build
  cache_directories:
    - env
    - node_modules
    - plugins

test:
  override:
    - ./runtests
