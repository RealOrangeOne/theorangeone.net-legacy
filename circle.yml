machine:
  python:
    version: 3.4.2
  node:
    version: 5.10.1
  environment:
    BUILD_PRODUCTION: true

dependencies:
  pre:
    - ./build
  cache_directories:
    - env
    - node_modules

test:
  override:
    - ./runtests
